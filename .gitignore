# Python bytecode from local linting
__pycache__/
*.py[cod]

# IDE
.idea/
.vscode/
*.iml

# Firmware images — downloaded and built by CI (and by the local run recipe),
# never committed
*.bin
mp.bin
mp-4m.bin
firmware-fs.bin

# Wokwi CI token — never commit it. It lives only in the repo's
# Actions secrets as WOKWI_CLI_TOKEN.
*wok_*
WokwiCI.txt
.env
